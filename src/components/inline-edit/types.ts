export interface InlineStyles {
  container: string;
  input: string;
  inputError: string;
  errorIcon: string;
  errorMessage: string;
  popover: {
    container: string;
    header: string;
    label: string;
    maskIndicator: string;
    actions: string;
    button: string;
  };
}